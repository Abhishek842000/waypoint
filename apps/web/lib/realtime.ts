"use client";

/**
 * Placeholder for Phase 2. Will subscribe to org-scoped SSE/WebSocket
 * events. Must not be implemented as polling.
 */
export function useIncidentRealtime(_orgId: string | undefined) {
  return { connected: false };
}
